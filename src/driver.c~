/* hw6.c
 *
 * Ben Mitchell
 * 2015-10-04
 *
 * driver for image manipulation program; very minimal
 */

#include "menuUtil.h"

/* main is very simple, it just calls the menu function and then exits
 * when it returns.  All the work is done elsewhere.
 */
int main () {

  menuLoop(stdin, stdout);
 
  return 0;
}
